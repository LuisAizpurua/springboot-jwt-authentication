spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5434/data
    username: root
    password: sasa
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
